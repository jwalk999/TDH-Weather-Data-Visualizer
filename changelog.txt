v0.1.0 - original alpha release
    - added graph to plot yesterday's temperatures in Fehrenheit

v0.2.0
    - added graph to plot monthly normal temperatures for comparison
    - changed save command to not run if the files are already present
    - added changelog file
    - graphs now have color and are more visually appealing
    - changed the minimum temperature for the hourly graph to 30 degrees
    - added LaGuardia Airport, NYC to the station id as an example
    - removed unnecessary files
    - added location name to the graph, changes when location_name var is changed
    - !turned down the temperature!

v0.3.0
    - separated the scripts that collects the data and turns it into graphs
        - looks cleaner and runs more efficiently
    - changed the main data collection script into a callable function with station id and location name as parameters
    - changed the climate normals graph script to only run if the user hasn't ran it before
        - checks for the norms file in documents and skips it if it exists
    - added another graph script to create a precipitation graph with the same parameters as temperature graph
        - sometimes it doesn't rain, made sure that a blank graph is not generated
    - !turned down the temperature!

v0.4.0 - The Future Is Here!
    - cleaned up the program directory, organizing files into folders
    - integrated openmeteo to collect forecasting data for current date + 7 days
    - added daily forecast
        - collects temperatures (hi and low) and precipitation chance 
        - outputs them into a text file in an easy to read format
    - added hourly forecast
        - collects temperature (mean), precipitation chance, sunrise/sunset, and weather description (sunny, cloudy, etc)
        - outputs data into text file in an easy to read format
    - added global_params.py in Data folder
        - allows for centralized parameters that are used by various scripts (i.e. location, date)
        - structures variables so that they can be easily edited when a GUI is implemented, adding to user experience
    - added config.json for a separate way to alter the user's location, for future use
    - integrated existing descriptions.json written by GitHub user stellasphere 
        - https://gist.github.com/stellasphere/9490c195ed2b53c707087c8c2db4ec0c
        - Interprets weather codes into user-readable phrases (1 = clear, etc.)
    - deleted unnecessary files and tests
    - !turned down the temperature!

v0.5.0
    - removed all uses of Meteostat
        - The use case that I was using it for does not fit how the API works. The historical data they gather would constantly be 1 month+ out of date and fetching results for "yesterday" would constantly pull "None." Empty data frames cannot be graphed.    :)
    - changed major layout of the program for easier management
    - formatted primary .py files to make reading / maintenance easier
    - updated how the weather data is stored, changed from text file to a csv file
    - 